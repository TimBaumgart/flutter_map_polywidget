import 'package:flutter/material.dart';

void main() {
  runApp(
    PolywidgetApp(),
  );
}

class PolywidgetApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("sdf"),
    );
  }
}